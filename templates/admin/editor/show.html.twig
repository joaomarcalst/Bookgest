{% extends 'base.html.twig' %}

{% block title %}Editor{% endblock %}

{% block body %}
	<div class="w-50 mx-auto"></br>
	<h1>Editor</h1>
	<p>Details of the editor</p>

	<table class="table">
		<tbody>
			<tr>
			    <th>Id</th>
				<td>{{ editor.id }}</td>
			</tr>
		    <tr>
			    <th>Name</th>
			    <td>{{ editor.name }}</td>
            </tr>
		</tbody>
	    </table>
	    <a class="btn btn-outline-dark" href="{{ path('app_admin_editor_index') }}">Back to List</a>
	    <a class="btn btn-warning" href="{{ path('app_admin_editor_edit', {'id': editor.id}) }}">Edit</a>

	    {{ include('admin/editor/_delete_form.html.twig') }}
    </div>
    </br>
{% endblock %}

